<!DOCTYPE html>
<html lang="en">
  <head>
    {{>head}}
  </head>
  <body class="container">
    <h1 style="text-align: center">{{ title }}</h1>

    <div class="text-right">
      <a href="/add-goal" class="btn btn-primary"> Add a Goal </a>
    </div>

    {{#entries}}
    <div hidden>{{ _id }}</div>
    <div>Fitness Goal: {{ goal }}</div>
    <div>Start Time: {{ startTime }}</div>
    <div>End Time: {{ endTime }}</div>
    <div>
      {{^completed}}
        <label for="{{completed}}">Completed:</label>
        <input type="checkbox" id="completed" name="completed" value={{completed}} disabled>  
      {{/completed}}
      {{#completed}}
        <label for="{{completed}}">Completed:</label>
        <input type="checkbox" id="completed" name="completed" value={{completed}} checked disabled>  
      {{/completed}}
    </div>
    <div>Written by {{ user }} , on {{ startDate }}</div>
    <div>
      {{^completed}}
      <form action="" method="POST">
        <button type="submit"
                name="completeButton"
                value="{{_id}}"
                class="btn btn-submit btn-sm">Complete Goal</button>
      </form>
      {{/completed}}
      <a href="/update-goal/{{ _id }}">Update</a>
      <a href="/delete-goal/{{ _id }}">Delete</a>
    </div>
    <hr />
    {{/entries}}
    {{^entries}}
    No fitness goals have been set yet.
    {{/entries}}
  </body>
</html>
